# 一番賞專區 — 結團後版面規劃

## 情境

一番賞專區的品項也會有「即將開團 / 正在開團 / 已結團」三種狀態。結團後若全部混在同一頁，會不易分辨哪些還能抽、哪些已結束。

---

## 方案比較

| 方案 | 說明 | 優點 | 缺點 |
|------|------|------|------|
| **A. 子分頁** | 點「一番賞專區」後，下方再出現「即將開團 / 正在開團 / 已結團」三個按鈕，與主頁邏輯一致 | 狀態清楚、可沿用現有篩選與卡片樣式 | 多一列按鈕 |
| **B. 單一列表 + 視覺區隔** | 一番賞維持一頁，已結團卡片灰化或加「已結團」標籤，進行中排上面 | 一頁看完、改動小 | 混在一起，結團多時仍會偏長 |
| **C. 分區塊** | 頁面分兩塊：「進行中」區 + 「已結團」區，各有標題與卡片 | 結構清楚、已結團集中一區 | 需較多版面與捲動 |
| **D. 可摺疊已結團** | 進行中在上方，下方「已結團（N 筆）」可展開/收合 | 預設精簡、需要再展開 | 實作與互動較複雜 |

---

## 建議採用：方案 A（子分頁）

- 進入「一番賞專區」後，**第二列**顯示：`[ 即將開團 ] [ 正在開團 ] [ 已結團 ]`。
- 篩選邏輯與主頁相同（依開團日／結團日判斷狀態），一番賞資料依狀態顯示在對應子分頁。
- 已結團的品項集中在「已結團」子分頁，版面單純、使用者習慣與主頁一致。

---

## 版面示意（方案 A）

```
[ 一番賞專區 ] [ 即將開團 ] [ 正在開團 ] [ 已結團 ]   ← 主分頁（一番賞為當前）
        ─────────────────────────────────────
        [ 即將開團 ] [ 正在開團 ] [ 已結團 ]         ← 一番賞子分頁（僅在「一番賞專區」時顯示）
        ─────────────────────────────────────
        [ 卡片 ] [ 卡片 ] [ 卡片 ]
        ...
```

- 非一番賞時：不顯示子分頁，維持現狀。
- 一番賞時：子分頁預設可為「正在開團」或「即將開團」（若沒有則選「已結團」）。

---

## 實作要點（方案 A）

1. **狀態判斷**：一番賞列表仍用同一套 `resolveStatusByDate(p)` 判斷即將／正在／已結團。
2. **子分頁變數**：例如 `currentIchibanshoFilter = 'upcoming' | 'ongoing' | 'ended'`，預設可為 `'ongoing'`。
3. **getFiltered()**：當 `currentFilter === 'ichibansho'` 時，改為依 `currentIchibanshoFilter` 篩選 `ichibanshoProducts`。
4. **HTML**：在 tab 列下方、卡片區上方新增一列「一番賞子分頁」，僅在 `currentFilter === 'ichibansho'` 時顯示並綁定點擊切換 `currentIchibanshoFilter` 與重新 `renderCards()`。

---

## 已實作（改為單一列表 + 狀態標籤）

- **子分頁已移除**，一番賞專區改為**單一列表**，版面與主頁一致、不再多一列按鈕。
- **排序**：同一頁內依狀態排序——先「即將開團」、再「進行中」、最後「已結團」；同狀態內依開團日由新到舊。
- **每張卡片**在日期下方顯示一個**狀態標籤**（小圓角膠囊）：
  - **即將開團**（藍色系）
  - **進行中**（綠色系）
  - **已結團**（灰色系）
- 一眼可分辨哪些還能抽、哪些已結束，無需再點子分頁。
