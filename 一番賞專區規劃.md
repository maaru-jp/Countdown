# 一番賞專區 — 位置評估與規劃

## 一、您提議的位置：Tab 列「即將開團」左邊

在導覽列最左側新增「一番賞專區」按鈕，順序變為：

```
[ 一番賞專區 ] [ 即將開團 ] [ 正在開團 ] [ 已結團 ]
```

| 項目 | 說明 |
|------|------|
| **優點** | 與現有分類一致、一進頁面就能看到；點擊後邏輯可沿用現有 tab（例如篩選「一番賞」標籤或獨立列表）。 |
| **注意** | 手機版 tab 會變成 4 個，需確認按鈕不擠、可橫向捲動或換行（目前已有 `flex-wrap`）。 |
| **實作** | 在 `index.html` 的 `<nav class="tabs">` 最前面加一個 `data-filter="ichibansho"` 的按鈕，在 `app.js` 的 `getFiltered()` 裡對該 filter 篩選「一番賞」商品（例如依 `badge` 或新欄位 `category: 'ichibansho'`）。 |

---

## 二、其他可增加識別的位置（建議一併考慮）

### 1. Hero 區（Logo 下方）— **辨識度最高**

在 Logo 正下方加一行「一番賞專區」入口（按鈕或連結）。

| 項目 | 說明 |
|------|------|
| **優點** | 第一眼就看到、可做成主推區、不影響現有開團/結團分類。 |
| **缺點** | Hero 區會多一塊，需微調間距與樣式。 |
| **適合** | 若一番賞是主打，放這裡最能提升識別與點擊。 |

### 2. Header 右側（主題切換左邊）

在畫面右上、主題按鈕（天空藍 / 星空 / 櫻花）左側加「一番賞專區」連結。

| 項目 | 說明 |
|------|------|
| **優點** | 常駐、不佔 tab 空間；可做成文字連結或小按鈕。 |
| **缺點** | 容易被視為次要；若同時有「回後台」等連結，需排好順序。 |
| **適合** | 一番賞為輔助功能、希望保留現有 3 個 tab 不變時。 |

### 3. 雙層導覽（一般開團 vs 一番賞）

第一層：**[ 一般開團 ] [ 一番賞專區 ]**  
第二層（僅在「一般開團」時顯示）：即將開團 / 正在開團 / 已結團。

| 項目 | 說明 |
|------|------|
| **優點** | 一般團與一番賞完全分開，結構清楚。 |
| **缺點** | 版面與 JS 改動大，需決定一番賞底下是否再分子分類。 |
| **適合** | 一番賞品項多、且與一般團營運方式差異大時。 |

---

## 三、建議方案（可混搭）

1. **必做（符合您需求）**  
   - 在 **Tab 列「即將開團」左邊** 新增「一番賞專區」按鈕，點選後只顯示一番賞商品（依標籤或 `category` 篩選）。

2. **建議加做（提高識別）**  
   - 在 **Hero 區 Logo 下方** 加一個「一番賞專區」按鈕，點擊後可：
     - 直接切到同一頁的「一番賞專區」tab，或  
     - 連到獨立的一番賞頁面（若之後有獨立頁）。

3. **選做**  
   - 若希望一番賞「常駐可見」又不佔 tab：在 **Header 右側** 加一個「一番賞專區」文字連結，行為與 Hero 按鈕一致即可。

---

## 四、資料與篩選方式（需先決定）

要讓「一番賞專區」有內容，需先定義「哪些算一番賞」：

| 作法 | 說明 |
|------|------|
| **A. 用現有標籤** | 例如把 `badge` 新增一個值 `ichibansho`，後台可選「一番賞」，展示頁 filter 為 `ichibansho` 時只顯示該 badge。 |
| **B. 新增欄位** | 在商品資料加 `category: 'ichibansho'` 或 `isIchibansho: true`，試算表多一欄「一番賞（Y/N）」。 |
| **C. 獨立列表** | 一番賞用另一份資料來源或試算表分頁，不與一般團混在一起。 |

建議先採用 **A 或 B**，與現有開團/結團狀態並存：同一商品可同時是「一番賞」且「正在開團」，點「一番賞專區」時只顯示一番賞，點「正在開團」時仍顯示所有正在開團（含一番賞）。

---

## 五、實作順序建議

1. 後台與資料：決定一番賞識別方式（badge 或 category 欄位），並在後台表單可勾選或選擇。
2. 展示頁：在 Tab 列「即將開團」左邊新增「一番賞專區」按鈕，並在 `getFiltered()` 依上述欄位篩選。
3. （可選）Hero 區加「一番賞專區」入口，點擊後切到該 tab 並捲動至 tab 列。
4. （可選）Header 右側加連結，行為同 3。

若您確定要採用「Tab 左邊 + 某一個識別位置（例如 Hero）」，並決定一番賞用 badge 還是新欄位，我可以依此寫出具體的 HTML / CSS / JS 修改步驟或程式碼。
